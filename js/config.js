const Config = {
    // Khi chạy local: http://localhost:3000
    // Khi đã deploy lên Render: thay bằng link Render của bạn (ví dụ: https://vay-tieu-dung-api.onrender.com)
    BASE_URL: 'http://localhost:3000'
};
